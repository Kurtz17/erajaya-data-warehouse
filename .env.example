# Copy this file to .env and adjust the values for your local PostgreSQL setup.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=erajaya_dw
DB_USER=postgres
DB_PASSWORD=postgres

# Optional. If set, this full URL takes priority over the fields above.
# DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/erajaya_dw
